export const SUBTASK_DETAILS = {
  "W1-1": [
    "Run `npm init quasar` and select Vue 3 + TypeScript + Vite. Choose Pinia for state management and Vue Router during scaffold.",
    "Generate at start.spring.io with Spring Web, Spring Security, Spring Data JPA, PostgreSQL Driver, and Flyway Migration dependencies.",
    "Add ESLint with Vue plugin, Prettier for formatting, and Husky + lint-staged for pre-commit linting. Ensures consistent code style.",
    "Create GitHub repo with branch protection on main. Use main for production, dev for integration, and feature/* branches for tasks.",
  ],
  "W1-2": [
    "Core table storing all note data. UUID for id, INT for positions/dimensions, VARCHAR for color hex. deleted_at enables soft delete pattern.",
    "Partial index excludes soft-deleted rows, reducing index size and improving query performance for active notes listing.",
    "Supports the renormalization endpoint which ORDER BY z_index across all user notes. Without this index, renorm requires a full table scan.",
    "Flyway runs versioned SQL on startup. V1__init.sql creates tables. Future migrations (V2, V3...) alter schema without data loss.",
    "docker-compose.yml with postgres:16-alpine. Maps port 5432, sets POSTGRES_DB/USER/PASSWORD. Run `docker compose up -d` to start.",
  ],
  "W1-3": [
    "Define SecurityFilterChain bean with stateless session management. Permit /auth/** endpoints, require auth for /api/**. Add JWT filter before UsernamePasswordAuthenticationFilter.",
    "Use jjwt library. Access token has userId + roles, expires 15min. Refresh token is opaque, stored hashed server-side with 7-day expiry.",
    "Validates credentials against BCrypt hash. Returns access token in JSON body. Sets refresh token as httpOnly, Secure, SameSite=Strict cookie with 7-day maxAge.",
    "Reads refresh token from httpOnly cookie, validates hash against DB. Issues new access + rotates refresh token (old invalidated). Prevents reuse attacks.",
    "Deletes refresh token hash from DB. Sets cookie maxAge=0 to clear from browser. Access token remains valid until expiry (15min max window).",
    "Custom OncePerRequestFilter extracts JWT from 'Authorization: Bearer <token>' header only. Never reads from cookies or query params to prevent CSRF.",
    "SHA-256 hash stored in refresh_tokens table with user_id and expires_at. On each refresh, old hash deleted and new inserted. Stolen tokens are single-use.",
  ],
  "W1-4": [
    "Vercel auto-deploys on push to main via GitHub integration. No custom Action needed \u2014 Vercel handles build and CDN distribution.",
    "GitHub Actions workflow: checkout \u2192 setup Java 17 \u2192 mvn verify. Runs on push to main and PRs. Fails PR check if any test fails.",
    "Create .env.example with all required vars (DB_URL, JWT_SECRET, etc.) without real values. Add .env to .gitignore. Document in README.",
    "Vercel creates unique preview URLs for each PR. Team tests frontend changes before merging. Comments preview URL on the PR automatically.",
  ],
  "W2-1": [
    "GET returns all active notes for authenticated user (WHERE deleted_at IS NULL). POST creates new note with server-generated id, timestamps, and default position.",
    "PUT updates note fields (content, position, color, z_index). DELETE sets deleted_at timestamp (soft delete). Both verify ownership via userId from JWT.",
    "Service layer validates authenticated user owns the note before mutation. Throws 403 Forbidden if userId doesn't match note.userId.",
    "Spring Data JPA interface extending JpaRepository<Note, UUID>. Custom query methods: findByUserIdAndDeletedAtIsNull, findByIdAndUserId.",
    "Instead of DELETE FROM notes, set deleted_at = NOW(). All queries filter WHERE deleted_at IS NULL. Enables undo and audit trail.",
    "Every response includes server's updated_at value. Client sends this back on next PATCH for conflict detection. Server is single source of truth.",
  ],
  "W2-2": [
    "Single POST endpoint accepts batch of offline changes. Each patch includes client's last-known updated_at so server can detect if another device modified the note.",
    "Server loads current note from DB and compares timestamps. Core conflict detection \u2014 if timestamps diverge, someone else modified the note.",
    "Client has latest version or newer \u2014 safe to apply. Uses last-write-wins only when no actual conflict. Updates DB and returns accepted status.",
    "Another device modified this note while client was offline. Returns HTTP 409 with both client's and server's version for user resolution.",
    "Batch response groups: accepted notes (applied) and conflicts (need user decision). Client processes each group differently.",
    "Per-conflict resolution UI. User sees side-by-side diff and explicitly chooses which version to keep. No silent data loss.",
  ],
  "W2-3": [
    "Client monitors highest z_index in store. At 900, calls this endpoint proactively \u2014 well before integer overflow becomes a risk.",
    "SELECT * FROM notes WHERE user_id = ? AND deleted_at IS NULL ORDER BY z_index ASC. Gets all notes in current stacking order.",
    "Iterates sorted notes and assigns consecutive integers starting at 1. Preserves relative ordering while eliminating gaps.",
    "Response includes every note with its new z_index. Client replaces entire z_index state from response \u2014 no local computation needed.",
    "Client overwrites all local z_index values with server response. Ensures client and server are perfectly in sync after renormalization.",
  ],
  "W2-4": [
    "Mock NoteRepository with Mockito. Test CRUD logic, ownership validation (403 for wrong user), soft delete behavior, and timestamp handling.",
    "Three scenarios: client_updated_at > server (accept), < server (409 conflict), == server (accept). Verify correct response for each case.",
    "Test with gaps (1, 5, 100 \u2192 1, 2, 3), duplicates (1, 1, 2 \u2192 1, 2, 3), and single note edge case. Verify dense rank preserves order.",
    "Test JWT validation: valid token passes, tampered returns 401, expired returns 401, missing cookie on refresh returns 401.",
    "Full request/response testing with MockMvc. Verify HTTP status codes, JSON structure, and content-type headers for all endpoints.",
  ],
  "W3-1": [
    "Standard Vue 3 structure. Pages are route-level views, composables are reusable logic hooks, services handle API calls, types define TypeScript interfaces.",
    "Three routes with lazy-loaded components. Board route requires auth (guarded), login/register are public.",
    "Access token lives only as Pinia ref. Cleared on tab close. Never written to localStorage or sessionStorage to prevent XSS theft.",
    "Create axios instance with request interceptor that reads token from useAuthStore and sets Authorization: Bearer header on every API call.",
    "When any API call returns 401, interceptor tries to refresh token. If refresh succeeds, replays original failed request transparently.",
    "App.vue onMounted calls /auth/refresh. httpOnly cookie sent automatically by browser. If valid, new access token stored in Pinia \u2014 user stays logged in.",
    "Vue Router beforeEach guard checks useAuthStore.isAuthenticated. If false and route requires auth, redirect to /login with redirect query param.",
  ],
  "W3-2": [
    "Use QInput with rules prop for client-side validation. Email format, password min 8 chars, required fields. QForm handles validation on submit.",
    "On successful login, extract access token from JSON body and store in useAuthStore. Refresh token automatically stored by browser as httpOnly cookie.",
    "Registration endpoint returns same response as login (access token + refresh cookie). User immediately logged in without separate login step.",
    "Two-step logout: API call clears server-side refresh token and cookie, then Pinia store is reset. Router navigates to /login.",
  ],
  "W3-3": [
    "Main board div fills viewport (100vw x 100vh minus header). position: relative is positioning context for absolutely-positioned notes. overflow: hidden clips edges.",
    "Each NoteCard uses position: absolute with left: pos_x, top: pos_y from Pinia store. Moving a note updates these values.",
    "v-for iterates notes applying style.zIndex from store. Higher z_index renders on top. Clicking bumps z_index to bring to front.",
    "FAB calls POST /api/notes with center viewport coords plus random \u00b150px offset. Prevents new notes stacking exactly on top of each other.",
    "Delete calls DELETE /api/notes/{id}. On 200, remove from Pinia. Optimistic UI: remove immediately, rollback on error.",
    "Store maintains local maxZ counter. On note click, z_index = maxZ + 1 applied instantly. Backend PATCH happens asynchronously.",
  ],
  "W3-4": [
    "QInput type='textarea' inside NoteCard. Autofocus on create. Resizes with card. Click to edit, blur or Escape to exit editing mode.",
    "Composable watches note content ref. On change, resets 500ms debounce timer. When timer fires, PATCH /api/notes/{id}. Prevents excessive API calls.",
    "Every PATCH body includes client's last-known updated_at. Server compares against its timestamp. If they diverge, another device edited the note.",
    "If PATCH returns 409, auto-save pauses and shows toast with both versions. User picks 'Keep Mine' or 'Use Server'. Resolution triggers new PATCH.",
    "Small colored dot on note header while changes pending (debounce timer running). Disappears after successful PATCH. Visual save-in-progress feedback.",
  ],
  "W4-1": [
    "Composable attaches mousedown to drag handle, then mousemove/mouseup to document (not element). Document-level listeners ensure drag continues if cursor leaves note.",
    "Only the colored header bar initiates drag. Clicking textarea body enters edit mode instead. Clear separation of interaction modes.",
    "CSS cursor changes provide drag affordance. cursor: grab on header mouseenter, cursor: grabbing on mousedown. Reset on mouseup.",
    "During drag, clamp pos_x to [0, viewportWidth - noteWidth] and pos_y to [0, viewportHeight - noteHeight]. Prevents dragging off-screen.",
    "On mouseup, start 800ms debounce. If user drags again within 800ms, timer resets. Final position sent as single PATCH, not per-pixel.",
    "Position changes use same conflict-aware PATCH pipeline. If another device moved same note, user gets conflict toast for position change.",
  ],
  "W4-2": [
    "Small resize handle at bottom-right corner of each note. Visible on hover. Uses diagonal lines icon or cursor indicator for affordance.",
    "Standard resize cursor (diagonal double arrow) on hover. Provides clear visual signal that this area is resizable.",
    "On grip mousedown, capture initial mouse pos and note dimensions. On mousemove, calculate delta and update width/height. On mouseup, finalize.",
    "Clamp during resize. Minimum prevents notes too small to read. Maximum prevents single note dominating the canvas.",
    "Same debounce pattern as drag. Final dimensions sent as single PATCH after resize finishes. Includes updated_at for conflict detection.",
  ],
  "W4-3": [
    "Global keydown listener. Ctrl+N (Cmd+N on Mac) creates note at viewport center. preventDefault stops browser's 'new window' shortcut.",
    "When note is selected but not in text edit mode, Delete/Backspace triggers soft delete. Does not fire when typing in textarea.",
    "Escape exits any active state: closes text editing, deselects focused note, closes context menu. Returns to neutral board state.",
    "Intercept contextmenu event on NoteCard. Show QMenu with note-specific actions at cursor. Prevent default browser context menu.",
    "QMenu item expands to show 6 color swatches inline. Clicking a swatch immediately updates color in store and sends PATCH.",
    "Creates new note with identical content, color, and dimensions. Position offset +20px right/down to show it's a copy.",
    "Soft deletes and shows 3-second undo toast. If user clicks undo before timer, delete cancelled (restored from local state before sync).",
  ],
  "W4-4": [
    "On note click/focus, immediately set z_index to max + 1 in Pinia. UI updates instantly. Backend PATCH follows asynchronously.",
    "After optimistic update, PATCH with new z_index and updated_at. On 409 conflict, server's z_index takes precedence \u2014 visual order usually correct.",
    "Every time localMaxZ incremented, check if approaching renormalization threshold. 900 gives buffer before values get unwieldy.",
    "Automatically triggers server-side renormalization. No user interaction needed. Happens transparently in background.",
    "Server returns all notes with consecutive z_index (1, 2, 3...). Client replaces entire z_index state. localMaxZ resets to N.",
    "Predefined palette matching classic sticky note colors. Stored as hex values. Applied to note background. Default is yellow (#FFF9C4).",
  ],
  "W5-1": [
    "Initialize Dexie database with notes table schema matching backend. Primary key on id, indexes on user_id and updated_at for efficient queries.",
    "Every create/update/delete writes to IndexedDB first, then syncs to server. If offline, data persists locally. Timestamp enables conflict detection.",
    "Separate IndexedDB table tracking unsynced operations. Each entry records what changed, when, and full payload to replay.",
    "Listen for browser online event. When connectivity returns, batch all pending operations via sync endpoint. Process response for conflicts.",
    "For accepted notes, update IndexedDB with server's authoritative timestamps. Ensures local data matches server state exactly.",
    "For each conflict, show toast with both versions. Queue multiple conflicts \u2014 resolve one at a time to avoid overwhelming user.",
    "Side-by-side comparison showing what changed. Highlight differences in content, position, or color. Two clear action buttons.",
    "Send another PATCH with client's version and force flag. Server accepts client version and updates timestamp. IndexedDB stays as-is.",
    "Overwrite local state with server's version. Update both Pinia store and IndexedDB. UI reflects server data immediately.",
  ],
  "W5-2": [
    "Set pwa mode in quasar.config.js. Enables service worker generation, manifest linking, and Workbox integration. Dev mode uses generateInDevMode.",
    "manifest.json: app name, icons at 192px and 512px, theme color, standalone display mode (no browser chrome). Required for install prompt.",
    "Static assets (JS, CSS, images) from cache for instant load. API calls try network first, fall back to cache. Balances freshness with offline.",
    "If user navigates while fully offline and no cached response exists, show cached fallback page with app shell and 'offline' indicator.",
    "Capture beforeinstallprompt event, show custom 'Install App' button. On click, trigger native install dialog. Store choice to avoid repeat prompts.",
  ],
  "W5-3": [
    "Spring CORS config with explicit allowedOrigins for production Vercel URL only. Blocks unauthorized origins. credentials: true for cookies.",
    "Token bucket: 10 tokens/min per IP. Prevents brute-force login. Returns 429 Too Many Requests when exhausted.",
    "Per-user rate limit on sync. 30 req/min is generous for normal use but prevents runaway clients from hammering the server.",
    "Use jsoup to strip all HTML tags from note content on save. Prevents stored XSS. Defense in depth alongside Vue's template escaping.",
    "SameSite=Strict means cookie never sent on cross-site requests. Combined with httpOnly, refresh token protected against XSS and CSRF.",
    "All hosting options provide free SSL. Redirect HTTP to HTTPS. Secure cookie attribute requires HTTPS \u2014 won't be sent over plain HTTP.",
  ],
  "W5-4": [
    "Trigger Ctrl+N keyboard event, assert new NoteCard element appears in DOM with default position near canvas center.",
    "Simulate mousedown/mousemove/mouseup on note header. Reload page. Assert position matches drag destination coordinates.",
    "Use cy.intercept to block API calls (simulate offline). Edit content. Re-enable network. Assert sync called and note updated without conflict.",
    "While offline, edit locally AND modify server DB directly. Go online. Assert ConflictResolutionToast appears with both versions.",
    "Click 'Keep Mine' on conflict toast. Assert local version is now authoritative. Verify PATCH sent with force flag.",
    "Click 'Use Server'. Assert UI shows server content. Verify IndexedDB and Pinia store match server state.",
    "Create notes, click repeatedly to inflate z_index. Assert renormalization called at 900. Assert z values reset to consecutive integers.",
  ],
  "W6-1": [
    "CSS transition on mount: scale(0.8) to scale(1) with opacity 0 to 1 over 200ms. Uses Vue Transition component for enter/leave animations.",
    "On delete, leave animation: scale(1) to scale(0.9) with opacity fade. Wait for animation (200ms) before removing from DOM via Pinia store.",
    "Toolbar with action icons appears on mouseenter, fades on mouseleave. CSS opacity transition. Prevents clutter when not interacting.",
    "Fixed-position toast at bottom-right. Shows old vs new with highlighted diffs. 'Keep Mine' and 'Use Server' buttons. Auto-dismiss after resolution.",
    "When no notes exist, centered illustration with text hint. Guides new users. Disappears as soon as first note is created.",
    "On first render, pulsing skeleton rectangles at random positions. Replace with real notes when API responds. Masks network latency.",
  ],
  "W6-2": [
    "Multi-stage Dockerfile: Maven build then JRE runtime image. Push to container registry. Deploy via platform CLI or GitHub integration.",
    "Create Neon.tech project, get connection string. Configure Spring datasource URL/user/password via env vars. Enable connection pooling.",
    "HTTP(s) monitor at /actuator/health with 5-min interval. Keeps free-tier instance from spinning down. Free plan covers this.",
    "Detect first API call after page load. Show skeleton UI while waiting. If response takes over 2s (cold start), show 'Waking up server...' message.",
    "Actuator health endpoint with DB health indicator. Returns 200 if healthy, 503 if DB unreachable. Used by UptimeRobot monitoring.",
    "Enable HTTPS via platform's free SSL. Update Spring CORS allowedOrigins to prod URL. Set Secure on cookies. Redirect HTTP to HTTPS.",
  ],
  "W6-3": [
    "Wrap NoteCard import with defineAsyncComponent to code-split from main bundle. Cards load on demand when board renders. Reduces initial size.",
    "Track in-flight requests per note ID. If new PATCH triggered while one pending, cancel previous or queue. Prevents race conditions.",
    "Add visualizer plugin to vite.config.js. Build generates treemap HTML showing bundle composition. Identify largest deps for optimization.",
    "Configure quasar.config.js to import only used components. Reduces bundle by excluding QTable, QCalendar, etc.",
  ],
  "W6-4": [
    "Install @sentry/vue, initialize in main.ts with DSN. Captures unhandled errors, failed API calls, performance traces. Free tier: 5K events/mo.",
    "Install sentry-spring-boot-starter. Auto-captures exceptions, slow transactions, HTTP 5xx. Same Sentry project for full-stack tracing.",
    "Add springdoc-openapi-starter-webmvc-ui dependency. Auto-generates OpenAPI spec from annotations. Interactive Swagger UI for testing.",
    "Document: prerequisites (Java 17, Node 18, Docker), local dev setup, all env vars with descriptions, and deployment guide.",
    "Compile all 8 ADRs into markdown in /docs/adr/. Each follows standard template: Context, Decision, Consequences, Status.",
  ],
};
